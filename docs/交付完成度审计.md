# 交付完成度审计

> 审计日期：2026-08-11
> 审计基线：`codex/s3-references-navigation`（父基线 `main` / `498f8cf`）
> 目标：以《交付阶段实施规划》的 S0～S13 退出条件和 1.0 阻断门为唯一完成标准
> 状态说明：`已验收` 表示退出条件已有可复现证据；`部分完成` 表示已有实现但尚未满足阶段退出条件；`未开始` 表示尚无可验收实现。

## 1. 当前结论

当前仓库已经完成 B01～B04，并验收 S0～S3：除可安装的 Dev Preview 核心纵切面外，已交付统一 XML 符号索引、Java/Kotlin Mapper、配置、TypeAlias、模块作用域、精确引用、Provider/继承导航、基础 Inspection 与首组安全 Quick Fix。

当前版本不是 1.0，也未达到《交付阶段实施规划》定义的完整交付标准。S0～S3 已验收；S4～S13 尚未达到可验收状态；不得以 ZIP 可构建、Plugin Verifier 通过或单一真实 IDE 路径通过代替全阶段完成。

## 2. 阶段验收矩阵

| 阶段 | 当前状态 | 已有证据 | 未满足的主要退出条件 | 下一道阻断门 |
|---|---|---|---|---|
| S0 产品基线与语料 | 已验收 | 首批/交付行为矩阵、风险清单、ADR；两个 Maven 样例；Java/Kotlin K2/Spring Boot/多框架/六方言可构建语义语料；全部 P0 正反例；副屏 IDEA 2026.1 五模块导入且项目错误为 0 | 后续阶段新增语义时持续扩充黄金语料，不重新打开 S0 | 进入 S1 产品基础设施与生命周期门 |
| S1 插件骨架与流水线 | 已验收 | Gradle 9.3、IntelliJ Platform 插件 2.18.1、Java 21；设置、通知、异常隔离、四类可选依赖描述符；Checkstyle、JaCoCo、依赖锁；70 个平台测试；20/20 沙箱启停；5/5 可选依赖隔离；本机完整 ZIP 生命周期；GitHub Actions run 31473442604 全绿 | 后续阶段新增可选能力时持续复跑隔离与生命周期，不重新打开 S1 | 进入 S2 统一语义模型与增量索引门 |
| S2 统一模型与索引 | 已验收 | 含类型的 namespace/statement/resultMap/sql fragment 索引；Java/Kotlin K2 Mapper、继承泛型、参数/实体、注解、MapperScan、MyBatis/Boot 配置、TypeAlias 与模块作用域；141 个平台测试；10 万 statement 实际索引查询；90.49% 整体与 90.05% 核心行覆盖率；20/20 生命周期、5/5 可选依赖隔离、最低 261 Verifier 与副屏五模块导入通过 | 后续阶段新增语义时持续扩充模型与压力语料，不重新打开 S2 | 进入 S3 精确引用、基础检查和安全 Quick Fix 门 |
| S3 导航、引用与基础检查 | 已验收 | namespace/id/refid/resultMap 精确引用与 Find Usages；继承、注解 SQL、Provider 导航；六类基础检查；创建 XML/statement、添加 `@Param` 的 ModCommand preview/Undo；192 个平台测试；90.38% 覆盖率；261 GA Verifier；20/20 生命周期；5/5 依赖隔离；副屏 Java↔XML/refid 实机 | 后续阶段扩展引用或写入能力时持续复跑 S3 安全门，不重新打开 S3 | 进入 S4 参数、ResultMap、TypeAlias 与重构门 |
| S4 参数、ResultMap、TypeAlias、重构 | 未开始 | 无 | 全部退出条件未满足 | 参数语义模型与黄金语料 |
| S5 动态 SQL 与源映射 | 未开始 | 无 | 全部退出条件未满足 | 独立动态 SQL 中间表示和字符级 source map |
| S6 OGNL 语言 | 未开始 | 无 | 全部退出条件未满足 | 词法/语法/PSI/错误恢复和类型系统 |
| S7 SQL 与数据库元数据 | 未开始 | 无 | 全部退出条件未满足 | SQL 注入、方言与异步可取消元数据抽象 |
| S8 数据库生成与安全合并 | 未开始 | 无 | 全部退出条件未满足 | 预览、Undo、冲突停止、失败恢复和 100 组黄金测试 |
| S9 方法名 SQL、Wrapper、Join | 未开始 | 无 | 全部退出条件未满足 | 确定性语法和可编译生成结果 |
| S10 转换、格式化、日志、执行、测试 | 未开始 | 无 | 全部退出条件未满足 | 幂等格式化、日志还原与危险 SQL 二次确认 |
| S11 Spring、注解、Kotlin、框架、数据库 | 未开始 | 无 | 全部退出条件未满足 | 可选依赖隔离和兼容矩阵冒烟 |
| S12 MCP、设置、国际化、产品化 | 未开始 | 无 | 全部退出条件未满足 | 默认关闭的本地 MCP、设置迁移、隐私、许可证、SBOM、签名与回滚 |
| S13 性能、可靠性、跨版本、正式发布 | 未开始 | 2026.1/2026.2 Verifier 和单一 macOS 副屏路径已有早期证据 | 最近四个稳定大版本、三系统、100 万行夹具、100 次生命周期、覆盖率、安全审查、Alpha/Beta/RC 升降级均未完成 | 全矩阵 RC 门禁和签名 1.0 发布包 |

## 3. 1.0 阻断门现状

| 阻断门 | 当前判定 | 证据或缺口 |
|---|---|---|
| S0～S13 全部退出条件满足 | 未通过 | S0～S3 与 B01～B04 有完整批次证据；S4～S13 尚未全部通过 |
| 已知 P0/P1 为零 | 未通过 | 全产品能力尚未实现，不能对未实现范围宣称零缺陷 |
| 最近四个稳定 IDE 大版本 | 未通过 | 当前只验证 2026.1，并以前瞻方式验证 2026.2 |
| macOS、Windows、Linux 真 IDE | 未通过 | 当前只有 macOS 2026.1.4 的副屏人工路径 |
| 100 次启停/打开关闭/索引重建 | 未通过 | S1 已完成 20/20 次启停且本插件异常为 0；S13 的 100 次与索引重建组合尚未执行 |
| 100 万行/数千 Mapper/多模块/多数据源 | 未通过 | S2 已通过 10 万 statement 实际索引查询和多模块隔离，但尚未达到 S13 的 100 万行、数千 Mapper 与多数据源综合基准 |
| 所有写入可预览、撤销、冲突和失败恢复 | 未通过 | S3 首组创建 XML/statement 与添加 `@Param` 已满足；S8 数据库生成、安全合并及全产品写入框架尚未完成 |
| 核心语义覆盖率不低于 85% | 通过 | `index`、`model`、`resolve` 合计 1581/1760 行，即 89.83%；低于阈值时 `check` 失败 |
| 整体自动化覆盖率不低于 70% | 通过 | 2385/2639 行，即 90.38%；低于阈值时 `check` 失败 |
| 许可证、NOTICE、第三方清单、SBOM | 未通过 | Apache-2.0 LICENSE 已有；NOTICE、第三方清单和 SBOM 尚缺 |
| 签名、发布、升级、降级和回滚 | 未通过 | 尚无签名配置、发布工作流、版本标签和升降级验收 |

## 4. 实施顺序

严格按基础依赖推进，避免用后续功能掩盖前置缺口：

1. S0 已完成：完整行为矩阵、可构建语义语料、数据库夹具和来源边界。
2. S1 已完成：设置/通知/异常隔离/可选依赖描述符、静态质量门、依赖锁定和 ZIP 生命周期。
3. S2、S3 已完成；继续 S4：参数/ResultMap/TypeAlias 消费、补全与重构。
4. 完成 S5～S7：动态 SQL、source map、OGNL、SQL PSI 和数据库元数据。
5. 完成 S8～S11：安全生成、转换工具、Spring/Kotlin/框架/数据库扩展。
6. 完成 S12～S13：MCP、设置迁移、国际化、隐私、SBOM、签名、全矩阵性能与正式发布。

每个批次必须同时留下任务卡、自动化报告、Plugin Verifier 结果、真实 IDE 验收结果和风险更新；没有证据的状态一律不得写为“已完成”。
