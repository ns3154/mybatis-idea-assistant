# 风险清单

> 适用批次：S0～S3、B01～B04
> 更新规则：每次架构、依赖、索引策略或支持范围变化后同步更新
> 风险级别：影响与概率均按低、中、高、极高记录

## 1. 当前风险

| 编号 | 风险 | 概率 | 影响 | 触发信号 | 控制措施 | 验证证据 | 状态 |
|---|---|:---:|:---:|---|---|---|---|
| R-001 | IntelliJ 261 API 或 Gradle 插件配置变化导致工程无法构建 | 中 | 高 | 编译失败、弃用升级为错误、Plugin Verifier 阻断 | 每次提交验证 2026.1 GA；定时/手动验证 2026.2；公共 API 优先 | Gradle 综合质量门与 Plugin Verifier 矩阵 | 已控制：261 GA 已验证，262 前瞻已配置 |
| R-002 | 用简单类名或文件名关联，造成同名 Mapper 错跳 | 高 | 高 | 不同包同名接口跳到同一 XML | 只允许全限定名与 namespace 完全相等 | 同名不同包 Fixture | 已控制 |
| R-003 | XML 符号索引保存 PSI，重解析后产生失效引用 | 中 | 高 | 修改 XML 后导航异常或跳到旧位置 | 索引只存字符串键；查询后重新解析；跨异步边界使用稳定键或智能指针 | 修改、撤销、移动和删除测试 | 已控制 |
| R-004 | 编辑或导航时全项目文件扫描，导致 EDT 卡顿 | 高 | 高 | UI 冻结、线程转储出现文件遍历 | XML/配置使用增量索引；TypeAlias 使用平台类名索引与上下文缓存；查询可取消；EDT 不做文件 I/O | 10 万 statement 实际索引查询、查询预算、线程检查和副屏体验 | 已控制，继续观察 S13 大型工程 |
| R-005 | Dumb Mode 查询索引抛异常 | 高 | 中 | 索引期间出现错误通知或日志堆栈 | 显式检测 Dumb Mode；返回“索引未就绪”；竞态捕获 `IndexNotReadyException`；不得无界重试 | Mapper、配置、TypeAlias、导航与 Inspection 平台测试 | 已控制 |
| R-006 | 重复 statement 稳定键时静默选错目标 | 中 | 高 | 导航结果受文件顺序影响 | 解析层保留全部候选；唯一时才直达 | 平台测试与副屏幕沙箱候选弹窗 | 已控制 |
| R-007 | 正在编辑的不完整 XML 破坏整个索引任务 | 高 | 中 | 输入半个标签后其他文件也无法导航 | 逐文件保守解析；局部错误隔离；不解析实体；不把解析异常传播到 EDT | XML 符号/配置扫描与真实 FileBasedIndex 不完整语料测试 | 已控制 |
| R-008 | Maven 样例的失败语料被 MyBatis 生产配置加载，导致回归构建失败 | 中 | 中 | `mvn verify` 初始化 SqlSessionFactory 失败 | 失败 XML 放在独立 corpus，生产配置只注册正常 Mapper；测试断言隔离契约 | 样例 `mvn clean verify` | 已控制，待持续验证 |
| R-009 | Java 重载被误宣称为参数级精确关联 | 中 | 中 | 多个重载方法都显示为唯一、无歧义映射 | 正向导航静默返回“不支持的源”；反向导航保留全部同名候选；不做参数签名推断 | 正向/反向重载边界测试与 UI 文案 | 已控制 |
| R-010 | 可选 Kotlin、Spring、YAML 或 Database Tools 依赖污染核心类加载 | 中 | 高 | 禁用可选插件后本插件无法启动 | S1 将四类可选能力放入独立描述符，核心描述符不直接引用其实现 | 五组可选依赖隔离启动测试均为插件已加载、零插件错误、退出码 0 | 已控制 |
| R-011 | 参考商业插件形成代码、资源、名称或界面污染 | 低 | 极高 | 出现来源不明的相似实现或资产 | 仅依据公开规范、自有语料和许可兼容的公开资料独立设计；记录来源；禁止反编译和复制 | ADR、依赖/许可证清单、代码审查 | 已控制，持续审查 |
| R-012 | 引入开源代码但未履行许可证或署名要求 | 中 | 高 | 发布前无法说明代码来源或许可证 | 默认不复制参考实现；确需引入时先审许可证、记录版本和修改，再单独审批 | 第三方清单、SBOM、文件头 | 开放 |
| R-013 | 公开 GitHub 仓库泄露凭证、个人路径或 IDE 本地状态 | 中 | 高 | 提交 token、`.idea` 私有文件、构建缓存或绝对路径 | 推送前检查忽略规则与 staged diff；仓库不保存数据库密码；CI 使用 Secret | 全仓敏感模式与个人绝对路径扫描、忽略目录核对、`git diff --cached`、GitHub Secret scanning | 已控制，持续扫描 |
| R-014 | 没有真实 IDEA 261 环境，自动化通过但插件不能交互或发布包生命周期异常 | 中 | 高 | ZIP 构建成功但 gutter 不可见、导航失败或安装生命周期异常 | B01 在副屏幕真实沙箱验证导航；S1 在 IDEA 2026.1 GA 完成安装、重启、升级、禁用、启用、卸载、重装和再次重启 | B01-06 与 S1 验收记录、IDE 日志、插件目录和版本核对 | 已控制；跨版本生命周期仍由 S13 发布门验证 |
| R-015 | 仅索引 statement，无法区分“无 Mapper XML”和“statement 缺失” | 高 | 高 | 两种失败都退化为同一个空集合，上层只能猜测 | S2 用新的统一 XML 符号索引分别记录 namespace、statement、resultMap 与 SQL fragment；解析层返回类型化结果 | 空 Mapper、索引 ID 重建和类型化解析测试 | 已控制 |
| R-016 | XML 反向导航按简单类名或静默选中某个重载，产生错跳 | 中 | 高 | 同名接口或 Java 重载时跳到非预期方法 | B03 只按 namespace 完整 FQN 找接口，并保留所有同名重载候选 | B03 同名包、重载和多候选平台测试 + 副屏幕验收 | 已控制 |
| R-017 | 样例只在本地偶尔运行，插件改动破坏真实 MyBatis 项目后未被 PR 发现 | 中 | 高 | Gradle 门通过但 Maven 样例回归失败 | 将样例 `clean verify` 纳入每次提交/PR 的 CI 阻断门 | CI 配置与本地等价命令 | 已控制，持续观察远端运行 |
| R-018 | 缺失 statement Inspection 扩大推断范围，对普通接口或合法 MyBatis 方法产生误报 | 中 | 高 | 无 Mapper XML、重载、默认/静态方法、注解 SQL、`@Flush` 或重复 statement 被标黄 | 只有类型化结果 `StatementMissing` 才报告；仅检查接口直接声明的唯一抽象实例方法；其余场景保守静默；S3 Quick Fix 只附着在同一保守结果上 | B04/S3 正反例平台测试与副屏幕误报验收 | 已控制 |
| R-019 | Inspection 与 gutter 对同一方法重复触发索引查询，造成编辑器高亮延迟或索引风暴 | 中 | 高 | 大接口打开或编辑时查询次数无界放大、EDT 卡顿、日志重复刷新 | 复用 B02 增量索引；每个精确方法首次查询有明确上限，同一修改世代共享类型化结果；缓存按 Mapper 源文件、XML、索引、Dumb 状态和项目根精确失效；禁止项目扫描和无界重试 | 50 方法查询键/缓存命中测试、线程检查与副屏幕编辑体验 | 已控制，持续观察大型工程 |
| R-020 | IDEA 2026.1 GA 从磁盘安装时把必需的 Java 插件依赖误显示为“未知插件”，导致用户误判 ZIP 不可安装 | 中 | 中 | 安装确认框出现 `com.intellij.java` 未知依赖提示 | 依赖 ID 保持 JetBrains 官方规定的 `com.intellij.java`；目标构建、Gradle 与 `plugin.xml` 双重声明一致；以安装后类加载、索引初始化、重启持久化和完整生命周期作为验收事实 | S1 真实 IDEA 2026.1 GA 生命周期记录；2026.1.4 平台测试与 Plugin Verifier | 已控制，发布说明需保留已知提示 |
| R-021 | 多模块查询扩大到 projectScope，串入不可见模块的同名 Mapper、XML、配置或别名 | 中 | 高 | 无依赖模块的同名符号出现在导航或模型中 | 编辑器查询使用调用元素 resolve scope；XML 反向查询使用 XML 模块及依赖作用域；MapperScan 校验来源文件可见性 | HeavyPlatform 多模块 statement、配置、TypeAlias、MapperScan、反向导航和根变化测试 | 已控制 |
| R-022 | Boot/MyBatis 配置格式扩展导致错误识别或漏失效 | 中 | 中 | profile、列表、宽松命名、未保存编辑后结果错误 | properties/YAML 使用独立增量索引；只接受已登记键；不展开未解析占位符；合并结果确定性排序 | properties/YAML、profile、列表、宽松命名、占位符和未保存修改测试 | 已控制当前范围，新增配置键时重开 |
| R-023 | TypeAlias 包扫描重复枚举类索引或冲突时静默选首个 | 中 | 高 | 大项目查询延迟、大小写别名漏匹配、同名别名错跳 | 按上下文缓存类名目录；大小写不敏感；显式、默认、包与 `@Alias` 目标全部保留；作用域随模块变化失效 | 内置/显式/包/注解/冲突/缩写类名/缓存与多模块测试 | 已控制，继续观察大型工程 |
| R-024 | XML 引用按文本或全项目作用域解析，导致 refid/resultMap/namespace 错跳 | 中 | 高 | 同名符号、前缀 namespace 或不可见模块返回错误目标 | 引用只消费统一符号键和调用元素 resolve scope；按属性子范围建立引用；返回前复核 PSI | S3 引用、Find Usages、同名、多模块与未解析正反例 | 已控制 |
| R-025 | Provider 或注解 SQL 使用待移除/内部 Java PSI API，261 通过但后续版本失效 | 中 | 高 | Plugin Verifier 报 scheduled-for-removal 或升级后类加载失败 | 只使用 261 公共 API；每批跑最低 GA Verifier；发现待移除 API立即替换 | S3 最终 Plugin Verifier 与 Provider/引用平台测试 | 已控制，持续验证新 IDE |
| R-026 | 高误报 Inspection 默认启用，合法动态 MyBatis 项目持续标黄 | 高 | 高 | 非绑定 namespace、字符串调用、隐式参数名被当作错误 | 只默认启用可证明的缺失 XML、缺失 statement、重复 statement；namespace、未使用、缺失 `@Param` 默认关闭并写明边界 | 六类 Inspection 注册、默认开关、正反例和描述资源测试 | 已控制 |
| R-027 | Quick Fix 写错目标、覆盖内容或产生不可撤销的半成品 | 中 | 极高 | 多 XML 同名动作不可辨识、冲突后仍写入、Undo 不能完全恢复 | ModCommand preview；项目相对路径区分目标；智能指针与执行前复核；只读/失效/重复停止；单命令 Undo | 创建 XML/statement、添加 `@Param` 的 preview/execute/Undo/冲突测试 | 已控制当前 S3 写入范围 |
| R-028 | “未使用 statement”自动删除破坏动态或框架调用 | 中 | 极高 | 运行期字符串调用在静态引用中不可见但 XML 被删除 | 检查默认关闭；不提供删除，只定位精确 Mapper 接口 | 默认开关、无删除动作、定位导航平台测试 | 已控制 |
| R-029 | S3 导航和引用在大 Mapper 中触发无界查询或 EDT 卡顿 | 中 | 高 | 打开 100 方法 Mapper 时 P95 超标、取消无响应 | 复用增量索引和缓存；循环检查取消；100 方法/statement 冷热路径 P95 设置硬上限 | S3NavigationPerformanceTest 与副屏交互 | 已控制当前规模，S13 扩大到百万行 |
| R-030 | 沙箱累计日志滚动后生命周期脚本沿用旧行号，制造“未关闭”的假失败 | 中 | 中 | 多轮启停在实际 shutdown 后仍超时，日志行号突然从数万归零 | 每轮日志单独归档后重置共享沙箱日志；启动、加载、异常与 shutdown 只在本轮日志内判断 | 修复后重新通过 20/20 生命周期与 5/5 可选依赖隔离，原固定失败的第 15 次已通过 | 已控制 |

## 2. 阻断规则

出现以下任一情况时，不得关闭对应批次：

- 已知错误会跳到错误 Mapper 或错误 statement；
- XML 修改、撤销或移动后仍使用陈旧目标；
- 索引期间出现未捕获异常或 EDT 明显卡顿；
- 构建产物依赖未声明的本地文件、个人绝对路径或本机缓存；
- Plugin Verifier 出现新增阻断；
- 公开仓库包含凭证、数据库口令、私有样例或来源不明的第三方代码；
- 样例 `mvn clean verify` 失败。
- B02 无法稳定区分“无 Mapper XML”和“statement 缺失”，或索引版本升级后仍读取旧缓存；
- B03 在同名接口或 Java 重载场景静默选择单一目标；
- B04 对无 Mapper XML、重载、默认/静态方法、注解 SQL 或 `@Flush` 产生误报，或尝试自动修改 XML；
- B04 Inspection 退化为全项目扫描、无界重试，或未建立查询性能守门；
- S2 XML/配置索引器创建 PSI、解析外部实体或在索引中查询项目类；
- S2 在无依赖模块间串联 Mapper、XML、配置、MapperScan 或 TypeAlias；
- S2 10 万 statement 实际索引查询、未保存编辑、Kotlin K2 或可选依赖隔离回归失败；
- 最低 2026.1 GA 兼容验证失败。
- S3 任一多目标引用或导航静默选中第一个，或跨不可见模块串联；
- 默认检查对合法动态 namespace、字符串调用或隐式参数名产生系统性误报；
- Quick Fix 缺少预览/单次 Undo，目标路径不可辨识，或在只读、冲突、失效场景仍写入；
- “未使用 statement”出现自动删除动作；

## 3. 风险关闭要求

风险只有具备可复现证据时才能标记“关闭”：

1. 写明控制措施已经落在哪个实现或测试中；
2. 记录实际运行命令和结果；
3. 对 IDE 交互类风险补充目标版本的人工验收记录；
4. 若只是降低概率而未消除，状态应为“已控制”，不能写“关闭”；
5. 重新打开风险时保留历史原因，不覆盖旧结论。

## 4. 每次公开发布前的最低检查

```bash
# 插件工程
./gradlew check verifyPluginProjectConfiguration verifyPluginStructure verifyPlugin

# 独立样例
mvn --batch-mode --file samples/java-mybatis-minimal/pom.xml clean verify
mvn --batch-mode --file samples/semantic-corpus/pom.xml clean verify

# 启动与依赖隔离
./scripts/verify-sandbox-lifecycle.sh 20
./scripts/verify-optional-dependency-isolation.sh

# Git 提交范围与敏感内容由仓库维护者在推送前复核
git status --short
git diff --cached --check
```

每次提交/PR 的最低兼容基线为 IntelliJ IDEA 2026.1 GA；定时或手动任务补充验证 2026.2。真实发布还必须检查依赖许可证、SBOM、插件签名，并在目标版本复核 ZIP 的安装、禁用、卸载和重启；S1 的 2026.1 GA 生命周期证据不能替代后续版本的发布验收。
