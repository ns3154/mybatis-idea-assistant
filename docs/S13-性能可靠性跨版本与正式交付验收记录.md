# S13 性能、可靠性、跨版本与正式交付验收记录

> 日期：2026-08-19
> 分支：`codex/s12-productization`
> 当前结论：提交 `1d8cf9b` 的主 CI、三系统、五宿主矩阵和 CodeQL 通过，手动加固门失败；2026-08-19 当前工作区已通过独占本地统一候选门、加固生命周期 1/1 与可选依赖隔离 5/5。新 HEAD 远端、真实 IDE/数据库、同一 SHA 生命周期 100/100、远端 5/5 与真实签名候选仍是交付阻断，S13 不得标记完成。

## 1. 本地统一质量门

### 1.1 2026-08-19 当前候选

- 禁用构建缓存并强制重跑 `./gradlew clean check verifyPluginProjectConfiguration verifyPluginStructure verifyPlugin` 成功（`BUILD SUCCESSFUL`，2m37s）；103 个测试套件中的 744/744 平台测试通过，失败、错误和跳过均为 0。
- 整体行覆盖率为 15385/18230（84.39%）；整体 70% 与所有分区覆盖率硬门通过。
- Java/MyBatis 样例 8/8、语义语料 11/11、Gradle Spring 四模块 2/2 及 `bootJar` 通过。
- Checkstyle、本地化、SBOM、项目与插件结构通过；Plugin Verifier 对最低 `IU-252.28539.54` 判定 `Compatible`。
- 临时密钥签名、验签、篡改拒绝、归档身份、加固生命周期 1/1、可选依赖隔离 5/5 以及发布静态契约通过；候选 ZIP SHA-256 为 `bbffd60fe0a88b4be2f2d73806b27f98d9fa58aee2136defcb133f8b557b09a9`。
- 最新报告生成于 22:09:08，晚于最新源码 21:41:41，覆盖最终源码。

该结果只关闭当前工作区的本地自动化首轮门，不替代新 HEAD 主 CI/三系统/CodeQL/五宿主、同一 SHA 生命周期 100/100、远端 5/5、真实 IDE/数据库或生产签名。

### 1.2 历史快照

执行：

```bash
mvn --batch-mode --file samples/java-mybatis-minimal/pom.xml clean verify
mvn --batch-mode --file samples/semantic-corpus/pom.xml clean verify
./gradlew clean check verifyPluginProjectConfiguration verifyPluginStructure verifyPlugin
go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.7 .github/workflows/*.yml
bash -n scripts/verify-sandbox-lifecycle.sh scripts/verify-optional-dependency-isolation.sh
git diff --check
```

下列结果属于工作区新改动之前的历史本地快照：

- 97 个平台测试套件、635 个测试全部通过，失败、错误和跳过均为 0；
- 整体行覆盖率为 13830/16592（83.35%），整体 70% 与各阶段独立硬门全部通过；
- Java + MyBatis 最小样例 6/6 通过，完整语义语料 8/8 通过；
- Checkstyle main/test、项目配置、插件结构、中英文硬编码门、CycloneDX 校验和 Action 工作流语法全部通过；
- 同一任务图连续运行两次，第二次明确输出 `Configuration cache entry reused`。

本节 635 个测试和 83.35% 仅作历史快照；当前候选必须使用 1.1 节的 744/744 和 84.39% 证据，不得混用分母或把本地结果冒充为远端证据。

## 2. 最近四个稳定 IDE 大版本

该历史快照以 IntelliJ IDEA 2025.2.6.2 为最低编译 SDK，生成的插件描述为 `since-build=252` 且没有 `until-build`。Plugin Verifier 1.409 的当时结果为：

| IDEA 版本 | 实际构建 | 本地结果 |
|---|---|---|
| 2025.2.6.2 | `IU-252.28539.54` | 纯 `Compatible`，可动态启停 |
| 2025.3.6 | `IU-253.33813.25` | 纯 `Compatible`，可动态启停 |
| 2026.1.4 | `IU-261.26222.65` | 纯 `Compatible`，可动态启停 |
| 2026.2.0.1 | `IU-262.8665.337` | 纯 `Compatible`，可动态启停 |

2025.2 编译阶段暴露的旧 `@NotNull` 类型使用方式已调整为最低 SDK 可接受的声明注解；跨版本同步读动作统一走稳定的 `Application.runReadAction(Computable)` 封装。最终四档结果均没有兼容、内部、实验、计划移除或弃用 API 报告。

## 3. 性能、恢复与安全

- 新增 100 万行、2000 个 Mapper、10000 个 statement、四个模块命名维度和四个数据源命名维度的扫描夹具；逐 Mapper 校验全部符号，P95 低于 50ms 的硬断言通过；
- 新增本机 headless 算法门：30 个独立 XML 宿主首次语义补全 P95 小于 500ms，20 个约 2000 行动态 XML 的真实局部 Inspection P95 小于 1s，30 个已加载 DbTable/DasTable 的单表生成计划 P95 小于 3s；100 表形成 400 个候选文件，进度按表和文件持续单调更新且取消不进入后续 Planner。补全门不是新项目冷启动，单表门不包含数据库连接/刷新；全插件 EDT 单次小于 50ms、真实 UI 无冻结和稳定额外内存仍待副屏/独立机实测。
- 该夹具与既有基于 IntelliJ 测试框架的 HeavyPlatform 临时多模块可见性测试、10 万 statement FileBasedIndex 测试共同守门，不能替代真实大型项目中的 UI/EDT 观察；
- 全量测试在 2025.2 SDK 下复现并修复 MCP statement 预览在 Undo 后读取陈旧 PSI 范围的问题：索引 PSI 只用于确定文件，候选偏移改由当前未保存 `Document` 的新解析结果计算；
- 首轮 Windows 远端回归暴露两处测试夹具写死 Unix 绝对 JAR 路径；夹具改为从当前系统临时目录生成规范化绝对路径后，历史提交 `53e6353bb551f0283ffdf8601da18ef96ba3ad0a` 曾在 Windows、macOS 和 Linux 固定 runner 上复验通过；当前可信三系统远端基线已更新为 `1d8cf9b`。
- MCP 的意外运行期异常写入本机诊断日志，对客户端仍只返回无路径、无堆栈的资源化安全错误；
- 全仓源码与最终 ZIP 未发现本机绝对路径、GitHub token、私钥或发布 token 内容。

## 4. 可复现制品与供应链

当前本地候选 ZIP SHA-256 为 `bbffd60fe0a88b4be2f2d73806b27f98d9fa58aee2136defcb133f8b557b09a9`。下表为早于当前候选的历史可复现制品快照，连续两次重新生成的摘要完全相同：

| 制品 | SHA-256 |
|---|---|
| 插件 ZIP | `5df8a9337bab5daced4c11d4c79426be069bc744aa8d886f5842686a68bf005c` |
| CycloneDX JSON | `e16f1b2b86b2c0ea5dd1c08cc15a41820ebe3057c336d2a4dc12df75ff951903` |
| CycloneDX XML | `b32e0ec1b0058caba09147d310e5ebcd4a1dd04f47c7ca7c7c72aae0b3abe906` |

ZIP 只包含一个插件 JAR；JAR 内含 Apache-2.0 `LICENSE` 与 `NOTICE`。Gradle Wrapper 有 SHA-256，Gradle 依赖锁已按最低 SDK 重建，所有 GitHub Action 固定到提交 SHA，Linux runner 固定为 `ubuntu-24.04`。仓库已启用 main ruleset、Dependabot 漏洞告警/安全更新、Secret Scanning 与 push protection，并新增 CodeQL Java/Kotlin 工作流。main ruleset 当前强制主 CI、CodeQL 工作流、Code Scanning 结果以及 Linux、macOS、Windows 三个固定 runner 共六个状态检查，禁止直接以红灯提交合入默认分支。

## 5. 远端自动化基线

最近已推送提交 `1d8cf9b` 的四项常规门成功，但同一提交的手动加固门失败；当前工作区修复尚未取得远端复验：

| 门禁 | 运行 | 结果 |
|---|---|---|
| 主 CI | [31614514532](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31614514532) | 成功 |
| CodeQL | [31614514499](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31614514499) | 成功 |
| Linux/macOS/Windows 无界面回归 | [31614514482](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31614514482) | 三系统成功，生命周期/隔离按 PR 事件跳过 |
| 四个 IDEA 大版本 + Android Studio | [31614547982](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31614547982) | 五宿主成功 |
| 三系统 + 生命周期 1 次 + 可选依赖隔离 | [31614559184](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31614559184) | 三系统成功；生命周期和隔离失败，当前工作区已修复已知根因但尚未远端复验 |

以下 `afcf017` 运行保留为加固前历史基线：

| 门禁 | 运行 | 结果 |
|---|---|---|
| 主 CI（attempt 2）：双 Maven、652 个平台测试、覆盖率、结构与最低 Verifier | [31594698118](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31594698118) | 652/652，0 失败/跳过；整体行覆盖率 83.56%；最低 Verifier `Compatible` |
| CodeQL Java/Kotlin | [31594698139](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31594698139) | 工作流与 GitHub Code Scanning 检查均通过 |
| Linux/macOS/Windows 无界面回归 | [31594698126](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31594698126) | 三个固定 runner 均为 652/652，0 失败/跳过 |
| 四个 IDEA 稳定大版本 + Android Studio | [31594759669](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31594759669) | IDEA 2025.2.6.2、2025.3.6、2026.1.4、2026.2.0.1 与 Android Studio 2026.1.2.10 全部 `Compatible` |
| 三系统 + 生命周期 1 次 + 可选依赖 5/5 | [31594751472](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31594751472) | 历史脚本全部通过，仅作为加固前基线 |
| 生命周期 100 次 | [31595497036](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31595497036) | 旧脚本 100/100 通过；因未强校验真实 Inspection 结果、MCP、资源和精确进程身份，不能关闭加固后的最终门 |

在 `afcf017` 上，必需检查脚本已核对 13 个精确 context 全部属于同一候选 SHA 且成功。上述结果仍只是已推送基线的无界面自动化与 Plugin Verifier 证据，不替代当前工作区候选的新 HEAD 远端复验或真实 IDE 安装与交互证据。

## 6. 工作区新增候选资产

- `dynamic-sql-golden/v1` 三组版本化黄金已新增；行为版本不能静默覆盖。
- 四模块 Gradle/Spring Boot 样例已随本地候选通过 2/2 及 `bootJar`；五类数据库版本 SQL 仅验证文本存在性、版本标识和特征 token，不是插件产品解析、实库或驱动兼容证明。
- 大型结构夹具可在合成物理文件树中确定性生成四模块、五个数据源命名/目录维度、1024 表、1024 Mapper、5120 statement 与 20 个 schema 文件，并校验稳定指纹、安全合并和扫描闭环；这不是真实数据源、IDE 项目导入或 UI 证据。
- 生命周期与可选依赖脚本已加固；当前工作区真实 1/1 报告与 5/5 隔离已通过，仍需新 HEAD 的远端同 SHA 复验和随后 100/100。
- GitHub 四个发布环境、`v*` 标签保护和 immutable releases 已配置；真实签名 secret、环境审批、首次 Marketplace 人工初始化和实际发布均未发生。

## 7. 当前未关闭的阻断门

以下项目需要外部环境或新授权，当前未执行，也不能由本记录的自动化结果替代：

1. 在副屏或独立测试机完成四个 IDEA 版本及三系统的安装、索引、导航、检查、生成、Undo、禁用、启用、卸载、重装与重启；
2. 对最终候选先执行加固生命周期 1 次并审查报告，再在同一 HEAD 执行 100/100 与可选依赖 5/5，记录 Inspection、MCP、日志、精确进程和项目零改写；
3. 仅在 GitHub Environment UI 注入真实 `CERTIFICATE_CHAIN`、`PRIVATE_KEY`、`PRIVATE_KEY_PASSWORD`；如需 Marketplace，再向预发布/正式环境分别注入 `PUBLISH_TOKEN` 并完成环境审批；
4. 人工初始化首次 Marketplace 条目，以唯一 bootstrap 版本决定 `Hidden`，不把同一版本交给后续自动化重传；
5. 完成 Alpha→Beta→RC→正式候选及反向降级，核对设置迁移、默认离线、MCP 端口释放和项目零隐式修改；
6. 在新 HEAD 重跑主 CI、CodeQL、三系统、五宿主和加固生命周期，不把 2026-08-19 本地结果或 `afcf017` 历史远端结论冒充为新 HEAD 远端证据。

在以上证据全部取得前，S13 状态保持“部分完成”，版本保持 `0.1.0-SNAPSHOT`，不得创建正式标签、公开 Release 或 Marketplace 正式版。
