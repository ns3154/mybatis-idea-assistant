# 首批验收记录

> 验收日期：2026-08-09
> 插件版本：`0.1.0-SNAPSHOT`
> 目标平台：IntelliJ IDEA 2026.1.4 / Build 261.26222.65

## 已通过

| 验收项 | 命令 | 结果 |
|---|---|---|
| IntelliJ 平台测试 | `./gradlew check verifyPluginProjectConfiguration verifyPluginStructure verifyPlugin` | 63 个测试，0 失败、0 错误；项目配置、插件结构与 ZIP 构建通过 |
| B01 补充平台回归 | `./gradlew test --tests 'io.github.ns3154.mybatisassistant.navigation.MyBatisMapperLineMarkerProviderTest'` | 16 个测试，0 失败、0 错误；补充真实导航处理器、改名恢复和移动路径 |
| 工程质量门 | 同上 | `BUILD SUCCESSFUL`，无配置或结构阻断 |
| 插件二进制兼容 | 同上 | 对 `IU-261.22158.277` 为 `Compatible`，可动态启用或禁用 |
| MyBatis 真实样例 | `mvn --batch-mode --file samples/java-mybatis-minimal/pom.xml clean verify` | 4 个测试，0 失败、0 错误 |

本轮自动化覆盖：

- Mapper 全限定名与 XML namespace 精确关联；
- `select`、`insert`、`update`、`delete` statement；
- statement 缺失、namespace 不匹配和重复候选；
- 真实 gutter 扩展注册；
- Dumb Mode 安全降级；
- 取消传播；
- 损坏 XML 不入索引；
- XML `id` 修改和 XML 删除后的旧关联失效；
- XML 改名后的失效/恢复与 XML 移动后的新路径刷新；
- 缺失 statement Inspection 的真实扩展注册、中文资源、默认 `WARNING` 和问题范围；
- 无 Mapper XML、重复候选、重载、默认/静态方法、注解 SQL、`@Flush` 与 Dumb Mode 的保守静默；
- 50 方法精确查询预算、gutter/Inspection 缓存复用和未保存 XML 修改失效；
- Java + MyBatis + H2 运行链路与隔离失败语料。

## 构建产物

- 文件：`build/distributions/mybatis-idea-assistant-0.1.0-SNAPSHOT.zip`
- 大小：41 KiB（41,986 bytes）
- SHA-256：`1bc24a7e3666163375c4519974e931c55cad738a5959d290cd27c34d9bc2801f`

## 真实 IDE 验收

2026-08-09 在副屏幕启动 IntelliJ IDEA 2026.1.4 沙箱并打开 Maven 样例，避免占用用户主屏幕。验收结果：

| 场景 | 结果 |
|---|---|
| 真实 gutter | `UserMapper.findById` 与 `insert` 均显示导航入口 |
| 唯一目标 | 点击 `findById` gutter，直达 `UserMapper.xml` 对应 `<select id="findById">` |
| 多候选 | 临时加入相同 `namespace#id` 后，平台展示两个 XML 候选，未静默选中第一个 |
| 修改与撤销 | 将 statement `id` 改名后旧 gutter 消失；撤销后关联恢复 |
| XML 移动 | 平台回归确认索引刷新到移动后的新路径，旧路径不再作为目标 |
| XML → Java 唯一目标 | 点击 XML `findById` gutter，直达 Java `UserMapper.findById(long)` |
| XML → Java 重载 | 临时加入 `findById(String)` 后，平台展示 `long`、`String` 两个候选；未静默选中任一个 |

B01 的 Java → XML 核心纵切面据此标记“已完成”。

## B02/B03 完成证据

- B02：同一索引加入 namespace marker 并升级到版本 2；类型化区分唯一、缺失、多候选、索引未就绪、源失效和不支持的源；智能指针删除后安全失效。
- B03：XML `id` 属性值叶节点显示反向 gutter；精确 FQN、同名不同包、普通类、继承方法、重载候选、带前缀属性、Dumb Mode 和取消均有平台测试。
- 完整门禁共 38 个平台测试、4 个 Maven 测试，最低 2026.1 GA Verifier 为 `Compatible`；副屏幕沙箱未发现本插件新增错误日志。

## B04 验收状态（已完成）

B04 在 B01～B03 的精确解析语义之上完成，不改写上文已经完成的历史验收结论。

| 验收项 | 预期证据 | 当前状态 |
|---|---|---|
| Inspection 注册与缺失警告 | 真实扩展注册；仅 `StatementMissing` 在 Java 方法名产生 `WARNING` | 13 个 Inspection 平台用例通过；显示名、分组、HTML 描述与精确问题文案均从真实注册加载 |
| 保守边界与动态刷新 | 无 XML、唯一/重复匹配、重载、默认/静态方法、注解 SQL、`@Flush` 和 Dumb Mode 无误报；未保存 XML `id` 修改/恢复后警告同步出现/消失 | 13 个 Inspection 平台用例通过，动态刷新无需保存文件 |
| 取消与查询性能 | 取消异常继续传播；50 方法查询键与线性上限受控；同世代重复消费命中缓存，无关 Java 文件编辑不驱逐缓存 | 12 个性能/生命周期用例通过；匹配路径为 1N，无 XML 路径为 2N，第二消费者不增加查询 |
| 真实 IDE 交互 | 在 `UserMapper.java` 验证缺失警告出现、恢复后消失，以及已有 statement 无误报 | 仅在副屏幕验收：改名后 1 个黄色警告和精确 Problems 文案；恢复后 0 个问题且两个导航行标恢复 |

完整质量门为 63 个平台测试、4 个 Maven 测试和最低 2026.1 GA Plugin Verifier `Compatible`。副屏验收期间未发现本插件新增异常栈、索引风暴或明显卡顿；平台曾在项目结构设置对话框打开期间记录一次后台写动作等待，线程栈停留在 JetBrains `ShowStructureSettingsAction`，不包含本插件代码。

## 发布前仍需检查

- ZIP 的独立安装、禁用、卸载和重启生命周期；
- 插件签名、发布令牌与 Marketplace 元数据；
- 依赖许可证、NOTICE 与 SBOM 核对；
- 2026.2 定时/手动前瞻验证结果。
