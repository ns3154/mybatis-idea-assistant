# 功能矩阵

> 矩阵版本：0.4
> 范围：B01～B04 首批纵切面，以及 S2 统一语义模型与增量索引
> 语义原则：首批只做精确匹配；不以猜测换取“看起来能跳转”

## 1. 状态与优先级说明

- `P0`：首批纵切面不可缺少，错误会使核心结果不可信；
- `P1`：首批体验或稳定性的重要保障；
- `P2`：后续增强，不阻断首批交付；
- `语料就绪`：已有可供实现和测试使用的输入；
- `待实现`：行为已确定，尚未交付代码；
- `开发中`：实现已进入工作区，尚未通过本批统一质量门和人工验收；
- `已验收`：实现、自动化、兼容和真实 IDE 证据均满足对应任务卡退出条件；
- `后续`：明确不进入 B01。

## 2. B01 行为矩阵

| 编号 | 用户行为/能力 | 输入 | 成功输出 | 失败或边界行为 | 主要依赖 | 测试层 | 优先级 | 状态 |
|---|---|---|---|---|---|---|:---:|---|
| F-001 | 识别 Java Mapper 接口 | Java 接口 PSI 与全限定名 | 生成 Mapper 稳定键和本接口方法名集合 | 无全限定名、非接口或 PSI 失效时返回空，不缓存错误对象 | Java PSI | Fixture | P0 | 已实现 |
| F-002 | 区分同名 Mapper | 不同包下的同名接口 | 以全限定名得到两个独立 Mapper | 禁止仅按简单类名合并 | F-001 | Fixture | P0 | 已实现 |
| F-003 | 读取 XML namespace | 格式正确的 Mapper XML | 返回原始完整 namespace | 缺失、空值或编辑中 XML 返回无稳定键 | XML PSI/索引 | 单元 + Fixture | P0 | 已实现 |
| F-004 | 索引 statement | `select/insert/update/delete` 的 `id` | 建立含符号类型、namespace、id 的稳定键到文件位置索引 | 缺失 `id` 不入索引；单文件错误不终止其他文件索引 | F-003、FileBasedIndex | Fixture | P0 | 已验收：S2 已升级为统一 XML 符号索引 |
| F-005 | Mapper/XML 正常关联 | 全限定名与 namespace 相同 | Mapper 关联到对应 XML | 没有 XML 时返回空 | F-001、F-003 | Fixture | P0 | 已实现 |
| F-006 | 方法/statement 正常解析 | 方法名与 statement `id` 相同 | 返回唯一 statement 目标 | 零个目标返回空；不按前缀、大小写或相似度猜测 | F-004、F-005 | Fixture | P0 | 已实现 |
| F-007 | Java 方法导航到 XML | 唯一解析目标 | 光标定位到 statement 名称或开始标签 | 目标失效时取消导航并允许重试 | F-006、导航 API | 平台 + 人工 | P0 | 已完成：真实 gutter 与唯一目标已验收 |
| F-008 | namespace 不匹配 | Mapper 全限定名与 XML namespace 不同 | 返回“无关联”语义结果 | 不按简单类名、文件名或目录名兜底 | F-005 | Fixture | P0 | 已实现 |
| F-009 | statement 缺失 | namespace 正确但无同名 `id` | 返回“statement 缺失”语义结果 | 不跳到任意同名文本或其他 namespace | F-006 | Fixture | P0 | 已完成：类型化结果已验证 |
| F-010 | 重复 statement 候选 | 多文件产生相同 statement 稳定键 | 返回全部有效候选供上层选择 | 不静默采用索引返回顺序中的第一个 | F-004、F-006 | Fixture | P0 | 已实现 |
| F-011 | 文件变化后关联失效 | XML 修改、移动、删除 | 旧键失效，新键在索引更新后可查询 | 不保留失效 PSI 或陈旧导航位置 | 修改追踪、索引 | Fixture + 人工 | P0 | 已完成：修改/撤销人工验收，移动/删除平台测试通过 |
| F-012 | Dumb Mode 降级 | 索引尚未就绪 | 不阻塞、不扫描全项目，返回暂不可用 | 不抛 `IndexNotReadyException` 到用户界面 | DumbService | 平台 | P0 | 已实现 |
| F-013 | 查询可取消 | 项目关闭或用户取消后台任务 | 尽快终止并释放引用 | 不吞取消异常，不在 EDT 重试循环 | Progress API | 平台 | P1 | 已实现 |
| F-014 | 样例 Maven 构建 | 正常语料及隔离的失败语料 | `mvn clean verify` 成功 | 失败语料只作为测试输入，不被生产配置加载 | Maven、MyBatis、H2 | Maven 集成 + CI | P0 | 已完成：本地通过并纳入 CI |
| F-015 | XML 方法导航回 Java | XML statement 的 `id` | 按精确 namespace 返回 Java 方法候选 | 不按简单类名兜底；重载不静默选一个 | Java PSI、XML PSI | 平台 + 人工 | P1 | 已完成：唯一目标与重载候选已验收 |
| F-016 | 缺失 statement 检查提示 | Java 接口直接声明的唯一抽象实例方法，且对应 namespace 的 Mapper XML 已存在 | 仅在同名 statement 缺失时于方法名显示警告 | 无 Mapper XML、已有或重复 statement、重载、默认/静态方法、MyBatis 注解 SQL、`@Flush`、Dumb Mode 或失效源均不提示；不提供 Quick Fix，不写用户文件 | F-018、Inspection API | 平台 + 人工 | P2 | 已完成：自动化与副屏验收通过 |
| F-017 | namespace marker 索引 | 合法 `mapper/@namespace`，即使没有 statement | 在统一 XML 符号索引中建立 namespace 符号 | 不以 statement 是否存在推断 Mapper XML 是否存在 | 统一 FileBasedIndex | 平台 | P0 | 已完成：空 Mapper 与新索引重建已验证 |
| F-018 | 类型化解析结果 | Java Mapper 方法与索引状态 | 返回稳定的结果类型供导航和后续检查复用 | 不依赖异常文本；取消异常继续传播 | F-006、F-017 | 平台 | P0 | 已完成：七类结果与智能指针已验证 |

## 3. 首批精确语义

### 3.1 Mapper 键

```text
mapperKey = Java 接口全限定名
xmlMapperKey = XML mapper/@namespace 的完整字符串
```

仅当两个字符串完全相等时，Mapper 与 XML 才关联。首批不解析 type alias，不根据 XML 文件名推断 namespace，也不做简单类名兜底。

### 3.2 Statement 键

```text
methodKey = Java 接口全限定名 + "#" + Java 方法名
statementIndexKey = "STATEMENT" + NUL + XML namespace + NUL + statement/@id
```

`NUL` 是内部不可出现在用户段值中的分隔符；符号类型避免 statement、resultMap 和 SQL fragment 同名时串类。首批不做参数级 Java 重载消歧。正向 Java → XML 遇到重载时返回“不支持的源”且不显示 gutter，避免把单个重载误称为唯一映射；反向 XML → Java 遇到重载时保留全部同名方法，由平台展示候选。

### 3.3 B02 结果类型

以下类型是 B02 已实现并通过统一质量门的稳定语义：

解析层至少区分以下结果，避免上层依赖异常文本：

| 结果 | 含义 | 导航行为 |
|---|---|---|
| `唯一匹配` | 稳定键恰有一个有效目标 | 直接导航 |
| `无 Mapper XML` | namespace 无匹配 XML | 不导航 |
| `statement 缺失` | namespace 已匹配，方法名无 statement | 不导航 |
| `多候选` | 稳定键有多个有效目标 | 展示候选 |
| `索引未就绪` | Dumb Mode 或索引不可查询 | 静默降级并允许稍后重试 |
| `源已失效` | 查询期间 PSI 或项目生命周期失效 | 取消当前动作 |
| `不支持的源` | 调用方传入非本批支持的 Java 源 | 不导航，不尝试猜测 |

namespace、statement、resultMap 与 SQL fragment 写入新的统一 `FileBasedIndex`：`mybatis.idea.assistant.xml.symbol`。新索引 ID 使旧 B02 缓存自然重建；零个 statement 候选时，解析层仍能区分“没有对应 Mapper XML”和“Mapper XML 存在但 statement 缺失”。

## 4. 样例覆盖映射

| 语料 | 路径 | 覆盖功能 |
|---|---|---|
| 正常 Mapper/XML | `samples/java-mybatis-minimal/src/main` | F-003～F-007、F-014 |
| namespace 不匹配 | `samples/java-mybatis-minimal/src/test/resources/corpus/failures/namespace-mismatch` | F-008、F-014 |
| statement 缺失 | `samples/java-mybatis-minimal/src/test/resources/corpus/failures/statement-missing` | F-009、F-014 |
| 对应失败接口 | `samples/java-mybatis-minimal/src/test/java/.../fixtures` | F-001、F-008、F-009、F-014 |

样例 Maven 验证同时作为本地回归与 CI 阻断门；CI 配置只有通过统一质量门后才视为已交付。

## 5. S2 扩展与后续边界

S2 已在不改变首批精确匹配原则的前提下交付：

- Java/Kotlin K2 Mapper、继承泛型方法、参数与返回实体模型；
- `@Mapper`、`@MapperScan`/`@MapperScans`、XML namespace 与 MyBatis 配置来源证据；
- 一个接口多个 XML、重复声明保留候选和跨模块依赖可见性；
- MyBatis XML、Spring Boot/MyBatis-Plus 配置与 TypeAlias 查询；
- 10 万 statement 真实索引查询、未保存编辑、根变化、Dumb Mode 和取消传播。

Java 重载的参数级消歧、框架基类适配、完整引用、更多 Inspection、补全、重构和安全 Quick Fix 仍由 S3、S4 与 S11 独立验收。

## 6. B03 反向导航语义

```text
mapper/@namespace 完整值 ──精确 FQN──> Java 接口
statement/@id          ──精确方法名──> 当前接口声明的方法候选
```

- 只接受全限定名完全相等的接口，不按 XML 文件名或简单类名兜底；
- 一个同名方法时直接导航；Java 重载产生多个候选时全部保留并交由平台展示；
- 不把继承方法、参数签名推断或框架基类扩展混入 B03；
- Dumb Mode、无精确接口、源失效或 XML 结构不支持时不导航。

## 7. B04 缺失 statement 检查语义

B04 只消费 B02 的类型化解析结果；只有 `statement 缺失` 会注册编辑器问题。检查目标进一步收窄为 Java 接口直接声明的、没有重载的抽象实例方法，并排除默认方法、静态方法、MyBatis `Select/Insert/Update/Delete` 与对应 Provider 注解覆盖的方法，以及不需要 XML statement 的 `@Flush` 方法。

以下结果均保守静默：

- 没有对应 namespace 的 Mapper XML；
- 已有唯一 statement 或存在重复候选；
- 普通类、注解类型、重载方法及非抽象方法；
- 索引未就绪、源已失效或用户取消检查。

警告只标记 Java 方法名，B04 不推断 CRUD 标签、不提供 Quick Fix，也不创建或修改 XML。B04 批次的 63 个平台测试、Maven 样例回归、最低 2026.1 GA Plugin Verifier 和副屏幕真实 IDE 交互验收均已通过，F-016 据此标记“已完成”；S2 当前完整平台测试总数为 141。

## 8. 兼容与持续集成矩阵

| 门禁 | 触发方式 | 目标 | 状态 |
|---|---|---|---|
| Gradle 综合质量门 | 每次提交/PR | `check`、项目配置、插件结构、插件验证 | 已配置；本地等价门禁通过 |
| Maven 样例回归 | 每次提交/PR | `samples/java-mybatis-minimal` 的 `clean verify` | 已配置；4 个测试通过 |
| Maven 语义语料 | 每次提交/PR | `samples/semantic-corpus` 的六项目 `clean verify` | 已配置；全部 reactor 项目与 6 个契约测试通过 |
| 最低 IDE 兼容 | 每次提交/PR | IntelliJ IDEA 2026.1 GA | 已验证：`IU-261.22158.277 Compatible` |
| 下一版本前瞻 | 定时/手动 | IntelliJ IDEA 2026.2 | 已配置，作为非阻断前瞻信号 |

## 9. S2 统一语义模型验收矩阵

| 编号 | 能力 | 保守边界 | 验收证据 | 状态 |
|---|---|---|---|---|
| F-019 | namespace、statement、resultMap、SQL fragment 统一索引 | 不完整/无关 XML、实体、前缀和嵌套伪声明不入索引 | 单元、平台、10 万 statement 真实索引查询 | 已验收 |
| F-020 | Java/Kotlin Mapper、继承方法、参数和实体模型 | 普通接口、无精确证据和不支持源不认定为 Mapper | Java/Kotlin K2、泛型、注解和缓存测试 | 已验收 |
| F-021 | MyBatis/Boot 配置与 TypeAlias | 不猜测占位符、冲突别名保留全部目标 | XML/properties/YAML、宽松命名、未保存修改和别名冲突测试 | 已验收 |
| F-022 | 模块依赖可见性 | 不返回无依赖模块的同名 Mapper、XML、配置或别名 | HeavyPlatform 多模块依赖与根变化测试 | 已验收 |
| F-023 | 生命周期与缓存失效 | Dumb、关闭、失效源类型化降级；取消传播 | 未保存编辑、移动/删除、项目根、20/20 启停与 5/5 依赖隔离 | 已验收 |
| F-024 | S2 统一质量门 | 不用历史绿灯代替当前代码验证 | 141 测试、90.49%/90.05% 覆盖率、Maven 双语料、261 GA Verifier、副屏实机 | 已验收 |
