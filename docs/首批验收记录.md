# 首批自动化验收记录

> 验收日期：2026-08-09
> 插件版本：`0.1.0-SNAPSHOT`
> 目标平台：IntelliJ IDEA 2026.1.4 / Build 261.26222.65

## 已通过

| 验收项 | 命令 | 结果 |
|---|---|---|
| IntelliJ 平台测试 | `./gradlew test --rerun-tasks` | 13 个测试，0 失败、0 错误 |
| 工程质量门 | `./gradlew check buildPlugin verifyPluginProjectConfiguration` | 通过，无配置阻断 |
| 插件二进制兼容 | `./gradlew verifyPlugin` | `Compatible`，可动态启用或禁用 |
| MyBatis 真实样例 | `mvn --batch-mode --file samples/java-mybatis-minimal/pom.xml clean verify` | 4 个测试，0 失败、0 错误 |

本轮自动化覆盖：

- Mapper 全限定名与 XML namespace 精确关联；
- `select`、`insert`、`update`、`delete` statement；
- statement 缺失、namespace 不匹配和重复候选；
- 真实 gutter 扩展注册；
- Dumb Mode 安全降级；
- 取消传播；
- 损坏 XML 不入索引；
- XML `id` 修改和 XML 删除后的旧关联失效；
- Java + MyBatis + H2 运行链路与隔离失败语料。

## 构建产物

- 文件：`build/distributions/mybatis-idea-assistant-0.1.0-SNAPSHOT.zip`
- SHA-256：`c815dff1be6412c701ec37910e334f5c1bc7ccf04903411ae8471c7e8af8d2c9`

## 尚待人工验收

- 启动沙箱 IDE 并打开 Maven 样例；
- 实际点击唯一目标和多目标 gutter；
- 修改 statement 后执行撤销，确认关联恢复；
- 安装 ZIP 后验证启用、禁用、卸载和重启；
- 检查 `idea.log` 中无本插件新增异常或明显 EDT 卡顿。

在上述人工路径完成前，B01 仍保持“开发中”，不标记阶段完成。
