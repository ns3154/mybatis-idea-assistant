# 交付完成度审计

> 审计日期：2026-08-12
> 审计基线：`codex/s12-productization`
> 目标：以《交付阶段实施规划》的 S0～S13 退出条件和 1.0 阻断门为唯一完成标准
> 状态说明：`已验收` 表示退出条件已有可复现证据；`部分完成` 表示已有实现但尚未满足阶段退出条件；`未开始` 表示尚无可验收实现。

## 1. 当前结论

当前仓库已经完成 B01～B04，并验收 S0～S3；S4～S12 的主要代码路径已形成阶段快照。ResultMap `column` 补全/缺失映射 Quick Fix、S5 `v1` 黄金、S9 set-based 批量、Gradle/Spring Boot 样例、大型结构夹具及发布/生命周期加固已进入当前工作区。`afcf017` 仍是已确认的远端基线（652 测试、83.56%、三系统、五宿主和 CodeQL）；2026-08-12 当前工作区已通过一次独占本地统一候选门：`BUILD SUCCESSFUL`（2m25s），101 个测试套件中的 722/722 平台测试通过，15135/17958（84.28%）整体行覆盖率，所有分区覆盖率、SBOM、本地化、结构和最低 `IU-252.28539.54` Verifier 均通过，ZIP SHA-256 为 `85992cb50b0656c4745aac9eda68f0b18ec2d8ba64099c3caebdab1d423a87e9`。最新报告生成于 23:43:40，晚于最新源码 23:41:28，覆盖最终源码。本地与远端证据仍必须分层，不得把该本地结果冒充为新 HEAD 远端证据。

当前版本不是 1.0，也未达到《交付阶段实施规划》定义的完整交付标准。S0～S3 已验收；S4～S12 的当前候选本地统一门已通过，但新 HEAD 远端、加固生命周期 1/100、5/5 可选依赖隔离或副屏实机尚未收口；S12/S13 还缺真实签名、Environment 审批、首次 Marketplace 人工初始化及升降级。历史旧脚本 100/100 已通过，但不能关闭加固后的最终候选门。不得以 ZIP 可构建、五库文本/版本/token 夹具、合成物理文件树、Plugin Verifier 或旧 HEAD 无界面流水线代替真实数据库/数据源、IDE 项目导入、当前候选远端门和实机验收。

## 2. 阶段验收矩阵

| 阶段 | 当前状态 | 已有证据 | 未满足的主要退出条件 | 下一道阻断门 |
|---|---|---|---|---|
| S0 产品基线与语料 | 已验收 | 首批/交付行为矩阵、风险清单、ADR；两个 Maven 样例；Java/Kotlin K2/Spring Boot/多框架/六方言可构建语义语料；全部 P0 正反例；副屏 IDEA 2026.1 五模块导入且项目错误为 0 | 后续阶段新增语义时持续扩充黄金语料，不重新打开 S0 | 进入 S1 产品基础设施与生命周期门 |
| S1 插件骨架与流水线 | 已验收 | Gradle 9.3、IntelliJ Platform 插件 2.18.1、Java 21；设置、通知、异常隔离、四类可选依赖描述符；Checkstyle、JaCoCo、依赖锁；70 个平台测试；20/20 沙箱启停；5/5 可选依赖隔离；本机完整 ZIP 生命周期；GitHub Actions run 31473442604 全绿 | 后续阶段新增可选能力时持续复跑隔离与生命周期，不重新打开 S1 | 进入 S2 统一语义模型与增量索引门 |
| S2 统一模型与索引 | 已验收 | 含类型的 namespace/statement/resultMap/sql fragment 索引；Java/Kotlin K2 Mapper、继承泛型、参数/实体、注解、MapperScan、MyBatis/Boot 配置、TypeAlias 与模块作用域；141 个平台测试；10 万 statement 实际索引查询；90.49% 整体与 90.05% 核心行覆盖率；20/20 生命周期、5/5 可选依赖隔离、最低 261 Verifier 与副屏五模块导入通过 | 后续阶段新增语义时持续扩充模型与压力语料，不重新打开 S2 | 进入 S3 精确引用、基础检查和安全 Quick Fix 门 |
| S3 导航、引用与基础检查 | 已验收 | namespace/id/refid/resultMap 精确引用与 Find Usages；继承、注解 SQL、Provider 导航；六类基础检查；创建 XML/statement、添加 `@Param` 的 ModCommand preview/Undo；192 个平台测试；90.38% 覆盖率；261 GA Verifier；20/20 生命周期；5/5 依赖隔离；副屏 Java↔XML/refid 实机 | 后续阶段扩展引用或写入能力时持续复跑 S3 安全门，不重新打开 S3 | 进入 S4 参数、ResultMap、TypeAlias 与重构门 |
| S4 参数、ResultMap、TypeAlias、重构 | 部分完成 | 原 S4 快照保留；直接 `column` 补全及 READY/唯一单表/可写属性守门的缺失映射 Quick Fix 已纳入 722/722、84.28% 当前候选本地门 | 新 HEAD 远端；加固 1/100、5/5 隔离和副屏 preview/Undo 仍待验 | 关闭新 HEAD 远端与加固门，再在副屏完成 S4 最终验收 |
| S5 动态 SQL 与源映射 | 部分完成 | 原 S5 快照保留；`v1` 三组版本化黄金已纳入 722/722、84.28% 当前候选本地门 | 新 HEAD 远端；加固 1/100、5/5 隔离与副屏真实 IDEA 待验 | 关闭远端/加固门，再完成 S5 实机验收 |
| S6 OGNL 语言 | 部分完成 | 不可执行词法/Pratt 语法、不可变 AST、注入 PSI 与高亮；参数/bind/foreach 作用域；Java/Kotlin 类型；引用、补全、Find Usages、Rename 与低误报 Inspection；335 个全量测试；89.03% 整体、88.93% 核心、86.86% OGNL 覆盖率；Maven 双语料与 261 GA Verifier 通过 | 20/20 生命周期、5/5 可选依赖隔离与副屏真实 IDEA 尚待当前 S6 产物验证 | 恢复副屏后完成 S6 最终验收；代码开发转入 S7 |
| S7 SQL 与数据库元数据 | 部分完成 | 原 S7 快照保留；ResultMap `column` 补全和缺失映射保守计划已纳入 722/722、84.28% 当前候选本地门 | 新 HEAD 远端；加固 1/100、5/5 可选依赖隔离与副屏元数据变化/Undo 待验 | 关闭远端/加固门，再完成实机 |
| S8 数据库生成与安全合并 | 部分完成 | Standard/MyBatis-Plus 确定性模板；命名/字段/注释/类型/TypeHandler/主键/方言；稳定 ID 与 SHA-256 安全区；未保存文档事实源；全量计划、原生 diff、文件选择、TOCTOU、Local History、稳定单命令 Undo/Redo 与异常回滚；415 个测试；88.50% 整体和 88.43% generator 核心覆盖率；100 组黄金摘要、双 Maven 语料、261 GA Verifier、ZIP、真实动作注册与远端 CI 通过 | 20/20 生命周期、5/5 可选依赖隔离与副屏数据库树/对话框/diff/Local History 实机待收口 | 检测到副屏后关闭 S8 最终验收；代码开发进入 S9 |
| S9 方法名 SQL、Wrapper、Join | 部分完成 | 原 S9 快照保留；set-based 增量已纳入 722/722、84.28% 当前候选本地门；Wrapper 当前输出与 Maven 逐字绑定，Plus 3.5.17/Flex 1.11.8 真实编译运行 5/5 | 新 HEAD 远端；加固 1/100、5/5 隔离和副屏三个动作待验；可选动态条件仍未接入 Database Tools 两个用户动作 | 关闭远端/加固门，再关闭实机门 |
| S10 转换、格式化、日志、执行、测试 | 部分完成 | 日志交错/类型/截断与词法安全还原；幂等 XML 与单次 Undo；CREATE TABLE/SELECT/Java 转换；显式参数；独立事务、双确认、取消和脱敏；JUnit 4/5 骨架；551 测试；整体 84.51%、S10 89.75%、数据库适配 78.51%；261 Verifier、双 Maven 语料和 ZIP 通过 | 20/20 生命周期、5/5 Database Tools 可选依赖隔离和副屏转换/格式化/执行/测试骨架实机待收口 | 等待副屏与隔离最终验收；代码开发进入 S11 |
| S11 Spring、注解、Kotlin、框架、数据库 | 部分完成 | 注解参数/迁移；Spring 注入 gutter；Kotlin K2；三框架统一模型；达梦独立方言；Community JDBC、PasswordSafe；615 测试；整体 12517/14962（83.66%）、框架核心 87.01%、JDBC 核心 271/306（88.56%）；双 Maven、261 Verifier、结构、ZIP、CI run 31556596528 及 IDEA/Android 三档矩阵 run 31556638831 通过 | 生命周期、5/5 隔离和副屏安装/交互待收口 | 副屏可用后完成 S11 实机验收 |
| S12 MCP、设置、国际化、产品化 | 部分完成 | 原 S12 快照保留；发布链加固已纳入 722/722、84.28%、SBOM/本地化/结构本地门，临时密钥签名/验签/篡改拒绝/归档身份和 shell/生命周期/发布静态契约通过；四个受保护环境、`v*` 标签规则和 immutable releases 已配置 | 新 HEAD 远端门；真实 Environment secret/审批、生产签名、首次 Marketplace 人工初始化与副屏升降级待验 | 用户在 GitHub UI 配置 secret/审批，先跑非公开候选 |
| S13 性能、可靠性、跨版本、正式发布 | 部分完成 | `afcf017` 远端：652 测试、83.56%、三系统/五宿主/CodeQL；当前工作区本地：722/722、84.28%、样例、SBOM/本地化/结构、临时签名/篡改拒绝/归档身份与静态契约通过；四模块 Gradle 样例、五库文本/版本/token 夹具和合成物理文件树已纳入本地门 | 新 HEAD 远端；加固脚本 1 次审查后 100/100 与 5/5；三系统/四 IDE 实机、真实 DM8、生产签名和渠道升降级 | 先产出新 HEAD 远端证据，再请求外部环境配合 |

## 3. 1.0 阻断门现状

| 阻断门 | 当前判定 | 证据或缺口 |
|---|---|---|
| S0～S13 全部退出条件满足 | 未通过 | S0～S3 与 B01～B04 有完整批次证据；S4～S10 开发完成但最终门未闭合；S11 部分完成，S12～S13 尚未通过 |
| 已知 P0/P1 为零 | 部分通过 | 当前代码审查未保留已知未处理 P0/P1，且当前工作区本地统一门已通过；但新 HEAD 远端、三系统真实 IDE、加固 100 次和生产签名候选尚未执行，因此不能对完整交付范围宣称零缺陷 |
| 最近四个稳定 IDE 大版本 | 部分通过 | 当前候选本地最低 `IU-252.28539.54` Verifier `Compatible`；`afcf017` 对 2025.2.6.2、2025.3.6、2026.1.4、2026.2.0.1 的远端 Verifier 均为纯 Compatible；新 HEAD 四档远端和真实 IDE 待验 |
| macOS、Windows、Linux 真 IDE | 未通过 | 当前只有 macOS 2026.1.4 的副屏人工路径 |
| 100 次启停/打开关闭/索引重建 | 未通过 | 历史旧脚本在 `afcf017` 100/100 成功，但不能证明真实 Inspection/MCP/资源/精确进程守门；新 HEAD 必须先跑加固脚本 1 次审查，再跑同一 HEAD 100/100 |
| 100 万行/数千 Mapper/多模块/多数据源 | 部分通过 | `afcf017` 已过 100 万行/2000 Mapper/10000 statement 门；当前候选的合成物理文件树（四模块/五命名维度/1024 表/1024 Mapper/5120 statement）已纳入本地统一门；它不是真实数据源、IDE 导入或 UI/EDT 证据 |
| 所有写入可预览、撤销、冲突和失败恢复 | 部分通过 | 原阶段快照覆盖 S3/S4/S8～S12 写入；ResultMap 新 Quick Fix 的源文本/元数据 TOCTOU 与 ModCommand 已纳入当前候选本地门，副屏 preview/Undo 仍待；S10 数据库写入另有双确认、显式事务和失败回滚 |
| 核心语义覆盖率不低于 85% | 当前候选本地通过 | 当前候选所有分区覆盖率硬门通过；远端仍需对新 HEAD 复验，不沿用 `afcf017` 分母 |
| 整体自动化覆盖率不低于 70% | 当前候选本地通过 | 2026-08-12 当前工作区为 15135/17958（84.28%）；`afcf017` 远端为 83.56%，两者分层记录，新 HEAD 远端待验 |
| 许可证、NOTICE、第三方清单、SBOM | 通过 | Apache-2.0 LICENSE/NOTICE、第三方清单与 CycloneDX 1.6 JSON/XML 已落盘；可复现与敏感字段门禁通过，ZIP 不捆绑独立第三方运行时 JAR |
| 签名、发布、升级、降级和回滚 | 未通过 | 加固工作流、四个 Environment、标签保护和 immutable releases 已配置；尚无 secret、环境审批、真实密钥签名、首次 Marketplace 条目、公开版本与副屏升降级证据 |

## 4. 实施顺序

严格按基础依赖推进，避免用后续功能掩盖前置缺口：

1. S0 已完成：完整行为矩阵、可构建语义语料、数据库夹具和来源边界。
2. S1 已完成：设置/通知/异常隔离/可选依赖描述符、静态质量门、依赖锁定和 ZIP 生命周期。
3. S2、S3 已完成；ResultMap、S5 黄金、S9 set-based 批量与新样例/夹具已通过 2026-08-12 当前工作区本地统一门，推送前只需对最终 diff 做增量静态核对。
4. 推送新 HEAD 后关闭主 CI、CodeQL、三系统、五宿主和精确必需检查；不得沿用 `afcf017`。
5. 在同一新 HEAD 先运行加固生命周期 1 次并审查报告，再运行 100/100 与 5/5 可选依赖隔离。
6. 用户只需在 GitHub Environment UI 配置 secret 并批准部署；首次 Marketplace 人工初始化使用唯一版本，随后跑真实签名草稿候选。
7. 副屏或独立机可用后完成四 IDE、三系统、Database Tools/PasswordSafe、真实 DM8、15 分钟首次使用及升降级/卸载验收。

每个批次必须同时留下任务卡、自动化报告、Plugin Verifier 结果、真实 IDE 验收结果和风险更新；没有证据的状态一律不得写为“已完成”。
