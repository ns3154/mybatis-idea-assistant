# S1 插件骨架与流水线验收记录

> 验收日期：2026-08-11
> 开发沙箱：IntelliJ IDEA 2026.1.4 / Build 261.26222.65
> 本机安装目标：IntelliJ IDEA 2026.1 / Build 261.22158.277
> 当前状态：通过，S1 退出条件已满足

## 自动化与静态质量

| 验收项 | 结果 |
|---|---|
| IntelliJ 平台测试 | 70 个测试，0 跳过、0 失败、0 错误 |
| Checkstyle | 主代码与测试代码均通过，零告警 |
| JaCoCo | 整体行覆盖率 432/483，即 89.44%；`index`、`model`、`resolve` 合计 249/288，即 86.46% |
| 覆盖率阻断门 | 整体不低于 70%、核心语义包不低于 85%，均通过；`check` 已依赖覆盖率校验 |
| 依赖可复现 | `gradle.lockfile` 已生成；Wrapper 已固定发行版 SHA-256 |
| ZIP 构建 | `0.1.0-SNAPSHOT` 与用于升级验证的 `0.1.1-SNAPSHOT` 均构建成功 |

2026-08-11 重新执行统一门禁：两个 Maven 工程均为 `BUILD SUCCESS`；Gradle 23 个任务全部执行成功，70/70 平台测试通过；最低支持的 IU-261.22158.277 Plugin Verifier 结论为 `Compatible`。

远端 GitHub Actions [run 31473442604](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31473442604) 全绿：两套 Maven 验证、Gradle 综合质量门、插件 ZIP 上传和验证报告上传均成功。

## 沙箱生命周期

`./scripts/verify-sandbox-lifecycle.sh 20` 结果：

- 20/20 次均加载 `MyBatis Assistant`；
- 20/20 次均记录 `IDE SHUTDOWN`；
- 所有进程退出码均为 0；
- 本插件异常计数均为 0。

`./scripts/verify-optional-dependency-isolation.sh` 结果：

| 场景 | 插件加载 | 本插件异常 | 退出码 |
|---|---:|---:|---:|
| 禁用 Kotlin | 1 | 0 | 0 |
| 禁用 Spring | 1 | 0 | 0 |
| 禁用 YAML | 1 | 0 | 0 |
| 禁用 Database Tools | 1 | 0 | 0 |
| 四类全部禁用 | 1 | 0 | 0 |

## 本机 IDEA ZIP 生命周期

全部操作均在副屏幕完成，只改动本插件；结束后 IDEA 已关闭。

| 步骤 | 证据 | 结果 |
|---|---|---|
| 安装 | 插件页显示 `0.1.0-SNAPSHOT` 已启用；日志记录动态加载；用户插件目录与 JAR 存在 | 通过 |
| 重启 | 启动日志列出 `MyBatis Assistant (0.1.0-SNAPSHOT)` | 通过 |
| 升级 | 安装 `0.1.1-SNAPSHOT` 后重启；JAR 描述符和启动日志均为新版本 | 通过 |
| 禁用 | `disabled_plugins.txt` 写入插件 ID；日志记录成功动态卸载 | 通过 |
| 启用 | 禁用文件清空；日志记录 21 ms 内动态加载 | 通过 |
| 卸载 | 用户插件目录消失；日志记录 classloader 检查后的成功卸载 | 通过 |
| 重装 | `0.1.1-SNAPSHOT` 目录、JAR 和插件 ID 恢复，禁用文件为空 | 通过 |
| 最终重启 | 启动日志再次列出 `MyBatis Assistant (0.1.1-SNAPSHOT)`，未发现本插件异常模式 | 通过 |

验收包：

- `mybatis-idea-assistant-0.1.0-SNAPSHOT.zip`：50,408 bytes，SHA-256 `b7ae2f535d4ae433206991954bd54265bdb0bf7ca83128c87778128c539b8be1`；
- `mybatis-idea-assistant-0.1.1-SNAPSHOT.zip`：50,410 bytes，SHA-256 `8597ac180b1bd12d3c0d9c8efda6a50563a67d32058747c574cd4451aba42534`。

## 平台提示说明

本机 IDEA 2026.1 GA 的“从磁盘安装”界面会先提示找不到 `com.intellij.java`，随后仍完成安装。Java 插件实际已由该 IDE 加载，且其描述符 ID 为 `com.intellij.java`；安装后的目录、动态加载、索引初始化、升级、重启、禁用、启用、卸载和重装均通过。项目继续采用 JetBrains 官方要求的 Java 插件依赖 ID，不为规避该 GA 界面提示改用非标准依赖。

依赖决策依据：

- [JetBrains：Plugin Dependencies](https://plugins.jetbrains.com/docs/intellij/plugin-dependencies.html)
- [JetBrains：IntelliJ IDEA Plugin Development](https://plugins.jetbrains.com/docs/intellij/idea.html)

## 后续阶段边界

- 100 次生命周期、最近四个稳定 IDE 大版本、三系统和签名发布属于 S13，不以本次 20 次 S1 证据替代。
