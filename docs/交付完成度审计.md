# 交付完成度审计

> 审计日期：2026-08-23
> 审计基线：本轮最终提交后的 `main` 精确 HEAD（以 `git rev-parse HEAD` 为准；不硬编码自身 SHA）
> 目标：以《交付阶段实施规划》的 S0～S13 退出条件和 1.0 阻断门为唯一完成标准
> 状态说明：`已验收` 表示退出条件已有可复现证据；`部分完成` 表示已有实现但尚未满足阶段退出条件；`未开始` 表示尚无可验收实现。

> 2026-08-23 收口复核：本轮当前 checkout 的 760/760 平台测试、84.40% 行覆盖率、项目/插件结构、本地化、SBOM、最低 IDE Verifier、两套 Maven 语料、Gradle 样例、临时签名链、真实生命周期 1/1 与 100/100、可选依赖隔离 5/5 和发布静态契约均已复核。已存在的 `v0.1.0-alpha.3` GitHub Release 保持公开 Pre-release；Marketplace 33742 的 alpha.3 保持 Hidden、Under review，本轮不重传、不改状态。下方历史分阶段数字不替代最终精确 SHA 的 GitHub 门禁，也不替代真实 IDE/数据库/跨系统验收。

## 1. 当前结论

当前 `main` 已完成 B01～B04，S0～S3 有完整验收记录；S4～S13 的当前 checkout 自动门已复核通过，分组测试为 326/326、329/329、55/55，S13 性能测试为 4/4。尚未由本机替代的证据是四档真实 IDE、Windows/Linux 真实 UI、真实 DM8/JDBC 环境、PasswordSafe/升级降级卸载实机，以及 GitHub 对最终提交精确 SHA 的远端运行结果；这些保持“外部待验”，不伪造为完成。

此前 `37bedad9` 的主 CI、CodeQL、三系统、五宿主和生命周期报告仍作为历史集成基线保留；任何后续代码、依赖或文档提交都必须在自己的精确 `main` SHA 上重新取得同一组门禁，不能沿用父提交绿灯。

当前版本仍不是 1.0，也未把 alpha.3 重传或改变 Marketplace Hidden 状态。S0～S3 已验收；S4～S13 自动门通过但外部实机/远端证据按上段标记。不得以 ZIP 可构建、五库文本/版本/token 夹具、合成物理文件树、Plugin Verifier 或历史父提交绿灯代替真实数据库、跨系统 UI、生产签名、来源证明和渠道验收。

## 2. 阶段验收矩阵

| 阶段 | 当前状态 | 已有证据 | 未满足的主要退出条件 | 下一道阻断门 |
|---|---|---|---|---|
| S0 产品基线与语料 | 已验收 | 首批/交付行为矩阵、风险清单、ADR；两个 Maven 样例；Java/Kotlin K2/Spring Boot/多框架/六方言可构建语义语料；全部 P0 正反例；副屏 IDEA 2026.1 五模块导入且项目错误为 0 | 后续阶段新增语义时持续扩充黄金语料，不重新打开 S0 | 进入 S1 产品基础设施与生命周期门 |
| S1 插件骨架与流水线 | 已验收 | Gradle 9.3、IntelliJ Platform 插件 2.18.1、Java 21；设置、通知、异常隔离、四类可选依赖描述符；Checkstyle、JaCoCo、依赖锁；70 个平台测试；20/20 沙箱启停；5/5 可选依赖隔离；本机完整 ZIP 生命周期；GitHub Actions run 31473442604 全绿 | 后续阶段新增可选能力时持续复跑隔离与生命周期，不重新打开 S1 | 进入 S2 统一语义模型与增量索引门 |
| S2 统一模型与索引 | 已验收 | 含类型的 namespace/statement/resultMap/sql fragment 索引；Java/Kotlin K2 Mapper、继承泛型、参数/实体、注解、MapperScan、MyBatis/Boot 配置、TypeAlias 与模块作用域；141 个平台测试；10 万 statement 实际索引查询；90.49% 整体与 90.05% 核心行覆盖率；20/20 生命周期、5/5 可选依赖隔离、最低 261 Verifier 与副屏五模块导入通过 | 后续阶段新增语义时持续扩充模型与压力语料，不重新打开 S2 | 进入 S3 精确引用、基础检查和安全 Quick Fix 门 |
| S3 导航、引用与基础检查 | 已验收 | namespace/id/refid/resultMap 精确引用与 Find Usages；继承、注解 SQL、Provider 导航；六类基础检查；创建 XML/statement、添加 `@Param` 的 ModCommand preview/Undo；192 个平台测试；90.38% 覆盖率；261 GA Verifier；20/20 生命周期；5/5 依赖隔离；副屏 Java↔XML/refid 实机 | 后续阶段扩展引用或写入能力时持续复跑 S3 安全门，不重新打开 S3 | 进入 S4 参数、ResultMap、TypeAlias 与重构门 |
| S4 参数、ResultMap、TypeAlias、重构 | 自动门通过，外部待验 | 当前 checkout 的 S4～S6 分组测试 326/326、全量 760/760、84.40% 覆盖率通过；参数路径、ResultMap/TypeAlias 引用、Rename 冲突停止与 Undo 测试均在组内 | 副屏真实 preview/Undo、最终 SHA 的远端门 | 外部 IDE 复核 |
| S5 动态 SQL 与源映射 | 自动门通过，外部待验 | 动态 SQL/source map 定向测试与 S4～S6 分组 326/326 通过；黄金文件、取消、Dumb/失效源边界通过 | 副屏真实 XML 注入、回映射与编辑刷新 | 外部 IDE 复核 |
| S6 OGNL 语言 | 自动门通过，外部待验 | OGNL 解析、语义、引用、补全、Kotlin 与 XML 注入测试纳入 326/326 分组并通过 | 副屏真实高亮、补全、Rename/Find Usages | 外部 IDE 复核 |
| S7 SQL 与数据库元数据 | 自动门通过，外部待验 | S7～S10 分组测试 329/329、全量 760/760、Database Tools/JDBC 适配和 SQL PSI 测试通过 | Database Tools 实机模型变化、真实驱动/数据库与 Undo | 外部 IDE/数据库复核 |
| S8 数据库生成与安全合并 | 自动门通过，外部待验 | 生成计划、冲突、TOCTOU、原子写入、Local History/Undo 测试纳入 329/329 并通过 | 副屏数据库树、差异预览和失败恢复实机 | 外部 IDE 复核 |
| S9 方法名 SQL、Wrapper、Join | 自动门通过，外部待验 | 方法 SQL、set-based batch、Wrapper、Join 与取消边界纳入 329/329；Plus/Flex Maven 编译契约通过 | 副屏三类动作和真实框架版本交互 | 外部 IDE 复核 |
| S10 转换、格式化、日志、执行、测试 | 自动门通过，外部待验 | 转换、格式化、日志、执行策略和 JUnit 骨架纳入 329/329 并通过 | 副屏预览/Undo、真实数据库执行确认交互 | 外部 IDE/数据库复核 |
| S11 Spring、注解、Kotlin、框架、数据库 | 自动门通过，外部待验 | Kotlin/Spring/设置/MCP/JDBC 分组 55/55、语义语料 11/11、可选依赖隔离 5/5 通过 | Android Studio、Community JDBC/PasswordSafe 和真实框架安装交互 | 外部 IDE 复核 |
| S12 MCP、设置、国际化、产品化 | 自动门通过，外部待验 | 全量 760/760、SBOM、本地化、结构、发布静态契约、临时签名链通过；Database Tools 动作改为中英文资源键 | 生产签名/Environment、真实 MCP 客户端、Marketplace 渠道行为 | 外部发布/IDE 复核 |
| S13 性能、可靠性、跨版本、正式发布 | 自动门通过，外部待验 | 性能测试 4/4；真实沙箱生命周期 1/1、100/100；可选依赖隔离 5/5；样例、Verifier 和资源趋势通过 | 最终 SHA 的 GitHub CI/CodeQL/三系统/四 IDE、真实 DM8、升降级/卸载 | 外部环境复核 |

## 3. 1.0 阻断门现状

| 阻断门 | 当前判定 | 证据或缺口 |
|---|---|---|
| S0～S13 全部退出条件满足 | 未通过 | S0～S3 与 B01～B04 有完整批次证据；S4～S10 开发完成但最终门未闭合；S11 部分完成，S12～S13 尚未通过 |
| 已知 P0/P1 为零 | 部分通过 | 当前代码审查未保留已知未处理 P0/P1，`37bedad9` 本地与远端自动门全绿；但三系统真实 IDE、真实数据库和生产签名候选尚未执行，因此不能对完整交付范围宣称零缺陷 |
| 最近四个稳定 IDE 大版本 | 部分通过 | `37bedad9` 对 2025.2.6.2、2025.3.6、2026.1.4、2026.2.0.1 的远端 Verifier 均为纯 Compatible；真实 IDE 待验 |
| macOS、Windows、Linux 真 IDE | 未通过 | 当前只有 macOS 2026.1.4 的副屏人工路径 |
| 100 次启停/打开关闭/索引重建 | main 集成基线通过 | `37bedad9` 加固脚本 100/100 成功；100 轮逐轮 Inspection/MCP/清洁/退出通过，异常、插件错误、孤儿和退出码均为 0；后续发布候选的精确 `main` SHA 仍须重跑发布必需检查 |
| 100 万行/数千 Mapper/多模块/多数据源 | 部分通过 | 已推送基线已过 100 万行/2000 Mapper/10000 statement 门；当前候选的合成物理文件树（四模块/五命名维度/1024 表/1024 Mapper/5120 statement）已纳入本地统一门；它不是真实数据源、IDE 导入或 UI/EDT 证据 |
| 所有写入可预览、撤销、冲突和失败恢复 | 部分通过 | 原阶段快照覆盖 S3/S4/S8～S12 写入；ResultMap 新 Quick Fix 的源文本/元数据 TOCTOU 与 ModCommand 已纳入当前候选本地门，副屏 preview/Undo 仍待；S10 数据库写入另有双确认、显式事务和失败回滚 |
| 核心语义覆盖率不低于 85% | main 集成基线通过 | 当前候选所有分区覆盖率硬门通过，`37bedad9` 远端主 CI 成功；后续发布候选的精确 `main` SHA 仍须复验 |
| 整体自动化覆盖率不低于 70% | main 集成基线通过 | 2026-08-19 本地为 15389/18234（84.40%），`37bedad9` 远端主 CI 成功；后续发布候选的精确 `main` SHA 仍须复验 |
| 许可证、NOTICE、第三方清单、SBOM | 通过 | Apache-2.0 LICENSE/NOTICE、第三方清单与 CycloneDX 1.6 JSON/XML 已落盘；可复现与敏感字段门禁通过，ZIP 不捆绑独立第三方运行时 JAR |
| 签名、发布、升级、降级和回滚 | 未通过 | 加固工作流、四个 Environment、标签保护和 immutable releases 已配置；尚无 secret、环境审批、真实密钥签名、首次 Marketplace 条目、公开版本与副屏升降级证据 |

## 4. 实施顺序

严格按基础依赖推进，避免用后续功能掩盖前置缺口：

1. S0 已完成：完整行为矩阵、可构建语义语料、数据库夹具和来源边界。
2. S1 已完成：设置/通知/异常隔离/可选依赖描述符、静态质量门、依赖锁定和 ZIP 生命周期。
3. S2、S3 已完成；ResultMap、S5 黄金、S9 set-based 批量与新样例/夹具已通过 2026-08-19 当前工作区本地统一门，推送前只需对最终 diff 做增量静态核对。
4. `37bedad9` 已关闭主 CI、CodeQL、三系统、五宿主、隔离与加固生命周期 1/1、100/100，并通过 13 个精确必需检查聚合。
5. `main` 集成已完成；后续发布候选的精确 `main` SHA 必须重跑发布必需检查，不能沿用 `37bedad9` 的父提交证据。
6. 用户只需在 GitHub Environment UI 配置 secret 并批准部署；首次 Marketplace 人工初始化使用唯一版本，随后跑真实签名草稿候选。
7. 副屏或独立机可用后完成四 IDE、三系统、Database Tools/PasswordSafe、真实 DM8、15 分钟首次使用及升降级/卸载验收。

每个批次必须同时留下任务卡、自动化报告、Plugin Verifier 结果、真实 IDE 验收结果和风险更新；没有证据的状态一律不得写为“已完成”。
