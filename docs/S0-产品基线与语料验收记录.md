# S0 产品基线与语料验收记录

> 验收日期：2026-08-11
> 目标 IDE：IntelliJ IDEA 2026.1
> 当前结论：通过，S0 退出条件已满足

## 1. 自动化结果

| 项目 | 命令 | 结果 |
|---|---|---|
| 首批真实样例 | `mvn --batch-mode --file samples/java-mybatis-minimal/pom.xml clean verify` | 4 个测试，0 失败、0 错误，`BUILD SUCCESS` |
| 完整语义语料 | `mvn --batch-mode --file samples/semantic-corpus/pom.xml clean verify` | 5 个模块全部成功；6 个契约测试，0 失败、0 错误 |
| 插件平台回归 | `./gradlew test` | `BUILD SUCCESSFUL`；63 个既有测试保持通过（任务命中 Gradle 缓存） |
| 配置与差异检查 | `git diff --check`；Ruby 解析 CI/Dependabot YAML | 无空白错误，`YAML OK` |

语义语料已实际覆盖：

- Java 21 Mapper、跨模块 Spring Boot/MyBatis XML；
- Kotlin 2.3.20 K2 源码编译和 1 个 XML 契约测试；
- MyBatis 3.5.19、MyBatis Spring Boot 4.1.0；
- MyBatis-Plus 3.5.17、MyBatis-Flex 1.11.8、TkMapper 6.0.0；
- MySQL、PostgreSQL、Oracle、SQL Server、SQLite、达梦；
- 13 项 P0 能力各一条正常和失败行为语料。

## 2. 缺陷发现与修复

首次构建显示 Kotlin 模块 `No sources to compile`，属于空绿；已为 Kotlin Maven 执行显式绑定 `src/main/kotlin` 和 `src/test/kotlin`。真实执行后又发现 Kotlin data class 构造参数与 ResultMap 不匹配，已收窄结果对象构造器并由 MyBatis XML 解析测试锁定。修复后 Kotlin 测试实际执行并通过。

## 3. 副屏 IDEA 验收（通过）

- 已将 IntelliJ IDEA 欢迎窗口固定到外接副屏 `VX2780-4K-hd` 后打开语义语料；
- 只信任 `semantic-corpus` 当前工程，未勾选信任整个 `samples` 目录；
- IDEA 已识别 `contracts-java`、`spring-application`、`kotlin-k2`、`framework-adapters`、`dialect-fixtures` 五个 Maven 模块；
- Maven 依赖解析和项目分析完成；Problems 的“项目错误”为 `0`；
- 验收结束后已关闭项目窗口和欢迎窗口，未继续占用主屏或副屏；
- 项目只产生被 `.gitignore` 覆盖的 `.idea` 与 `target`，未改写受 Git 跟踪的源码。

## 4. 退出结论

- 完整行为矩阵已固定 P0/P1/P2 的输入、成功输出和保守失败语义；
- 13 项 P0 均有机器校验的正常/失败语料；
- 两个 Maven 样例、Kotlin K2、三框架和六方言夹具均可构建；
- 目标 IDE 导入、模块识别和项目错误检查通过；
- S0 无已知 P0/P1，准予进入 S1。
