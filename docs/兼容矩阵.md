# 兼容矩阵

> 更新日期：2026-08-12
> 说明：自动化、Plugin Verifier 与真实 IDE 是三类独立证据；“自动化通过”不等于“真实安装已验”。

## IntelliJ Platform

| 产品 | 版本 | 当前自动化 | 当前真实 IDE |
|---|---|---|---|
| IntelliJ IDEA | 2025.2.6.2 | 最低编译 SDK；本地与远端均为纯 `Compatible` | 待独立测试环境 |
| IntelliJ IDEA | 2025.3.6 | `IU-253.33813.25`；本地与远端均为纯 `Compatible` | 待独立测试环境 |
| IntelliJ IDEA | 2026.1.4 | `IU-261.26222.65`；本地与远端均为纯 `Compatible` | 历史阶段有 macOS 副屏证据，最终产物待复核 |
| IntelliJ IDEA | 2026.2.0.1 | `IU-262.8665.337`；本地与远端均为纯 `Compatible` | 待独立测试环境 |
| Android Studio | 2026.1.2.10 | 远端核心/降级 Plugin Verifier 通过 | 待独立测试环境 |

远端五宿主证据为 [GitHub Actions 31570824593](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31570824593)。插件使用 `since-build=252` 且不限制 `until-build`；不限制上限不是未来版本兼容承诺，新稳定版必须进入矩阵后再更新支持说明。

## 操作系统

| 系统 | 后台自动化 | 安装/编辑/生成/卸载 |
|---|---|---|
| Linux `ubuntu-24.04` | 当前提交无界面回归通过 | 待独立测试机 |
| macOS `macos-15` | 当前提交无界面回归通过 | 最终产物待副屏复核 |
| Windows `windows-2025` | 当前提交无界面回归通过 | 待独立测试机 |

三系统后台证据为 [GitHub Actions 31570715199](https://github.com/ns3154/mybatis-idea-assistant/actions/runs/31570715199)。这些结果覆盖路径、换行、构建、测试和插件结构差异，不等同于三系统真实 GUI 操作。

## 语言、框架与宿主能力

- Java Mapper、XML、OGNL 和核心索引为基础能力。
- Kotlin K2、Spring、YAML 和 Database Tools 通过可选描述符隔离；缺少可选插件时核心能力必须继续加载。
- MyBatis-Plus 3.5.17、MyBatis-Flex 1.11.8 与 TkMapper 按锁定语料验证；运行项目版本超出已验范围时只使用可证明的公共语义，不伪造支持。
- Database Tools 不可用时可使用显式 Community JDBC 元数据；插件不捆绑或下载 JDBC 驱动。
- 支持 MySQL、PostgreSQL、Oracle、SQL Server、SQLite、H2 与达梦方言模型；实际数据库版本、驱动和权限仍由用户环境决定。

## 明确不保证

- 未进入本表的 IDE、EAP、第三方 IDE 分支或非标准 JRE。
- 动态运行期拼接的 namespace、statement id、表名、列名、Map 键和任意 OGNL 结果。
- 未显式选择的数据源、框架、版本、外键关系或生成目标。
- 未签名开发 ZIP 的生产分发、自动更新与 Marketplace 兼容性。
